// DEPRECATED — unused, not imported anywhere.
// Superseded by PricingSection.tsx (also currently unused — see audit notes).
// Safe to delete this file.
export default function PricingDeprecated() {
  return null
}
