// DEPRECATED — unused, not imported anywhere.
// Superseded by ServicesSection.tsx (used on the homepage) and the
// dedicated /services + /services/[slug] pages. Safe to delete this file.
export default function ServicesDeprecated() {
  return null
}
